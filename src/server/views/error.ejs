<% if (!user) { %>
    <h2>Something went wrong - please try to sign in again!</h2>
    <p><a href="auth/steam">Sign On with Steam</a></p>
    <% } else { %>
        <h2>Hello, <%= user.displayName %>. You can close this window or <a href='logout'>Logout</a></h2>
        <% } %>
